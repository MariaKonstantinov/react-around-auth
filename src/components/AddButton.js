import React from "react";
import "./AddButton.css";

function AddButton() {
  return (
    <button
      type="button"
      className="button button_type_add"
      aria-label="add"
    ></button>
  );
}

export default AddButton;
